This is a simple ansible playbook for configuring an ubuntu workstation to my liking

Constant work in progress

To install use 

`ansible-playbook -i inventory.yaml playbook.yaml -K`

Make sure to have a virtual environment set up & to edit the user value in the group_vars directory.
